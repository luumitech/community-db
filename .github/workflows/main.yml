name: Main branch

on:
  workflow_dispatch:
  push:
    branches:
      - main

env:
  NODE_VERSION: '24'

jobs:
  release:
    name: Create release
    runs-on: ubuntu-latest
    permissions:
      # needed to create tags and GitHub releases
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0  # needed to fetch all tags for version detection

      - name: Enable corepack to support yarn@4
        run: corepack enable

      - name: Use Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --immutable

      - name: Install release tools
        run: |
           yarn add release-it @release-it/conventional-changelog
           git config --global user.name "github-actions[bot]"
        env:
          YARN_ENABLE_IMMUTABLE_INSTALLS: false

      - name: Create release
        run: npx release-it --ci
        env:
          # A workflow triggered by GITHUB_TOKEN cannot trigger other workflows.
          # since release.yml should be triggered after every release creation
          # we must use our own repository secret
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN_SEMANTIC_RELEASE }}

  # Azure deployment uses up way too much github action minutes
  # deploy:
  #   name: Deploy to Azure
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@master

  #     - name: Use Node.js ${{ env.NODE_VERSION }}
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}

  #     - name: 'Deploy to Azure WebApp'
  #       uses: azure/webapps-deploy@v2
  #       with:
  #         app-name: ${{ env.AZURE_WEBAPP_NAME }}
  #         publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
  #         slot-name: production
