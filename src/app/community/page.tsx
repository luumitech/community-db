'use client';
import React from 'react';
import { Hero } from '~/view/landing/hero';

export default function CommunityLanding() {
  return <Hero className="min-h-[calc(theme(spacing.main-height)-0.5rem)]" />;
}
